{
  "enabled": true,
  "config-item-title": "Log ingest - k8s namespace: {{ .namespace }}",
  "send-to-storage": true,
  "matchers": [
    {
      "attribute": "k8s.namespace.name",
      "operator": "MATCHES",
      "values": ["{{ .namespace }}"]
    }
  ]
}
