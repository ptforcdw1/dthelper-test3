manifestVersion: "1.0"

# Reference manifest — lists all templates for local testing only.
# Jenkins generates manifest-run.yaml at runtime pointing to tasks/<taskID>/.

projects:
  - name: log-ingest-k8s
    path: templates/log-ingest-k8s
  - name: auto-tagging
    path: templates/auto-tagging
  - name: update-windows
    path: templates/update-windows
  - name: host-alerts
    path: templates/host-alerts

environmentGroups:
  - name: production
    environments:
      - name: ylq89164
        url:
          value: https://ylq89164.live.dynatrace.com
        auth:
          token:
            name: DT_API_TOKEN
