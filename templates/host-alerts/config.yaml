configs:
  - id: host-cpu-alert
    config:
      name: "host-cpu-alert"
      template: template.json
      parameters:
        alertTitle: "POC Host CPU Alert"    # EDIT: alert display title
    type:
      settings:
        schema: builtin:davis.anomaly-detectors
        scope: environment
