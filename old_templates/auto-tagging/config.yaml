configs:
  - id: auto-tagging-ownership-team
    config:
      name: "auto-tagging-ownership-team"
      template: template.json
      parameters:
        teamName: "POC Team"        # EDIT: team display name
        identifier: "poc-team"     # EDIT: unique identifier (lowercase, no spaces)
    type:
      settings:
        schema: builtin:ownership.teams
        scope: environment
