configs:
  - id: update-window-weekly
    config:
      name: "update-window-weekly"
      template: template.json
      parameters:
        windowName: "POC Update Window"    # EDIT: window display name
    type:
      settings:
        schema: builtin:deployment.management.update-windows
        scope: environment
